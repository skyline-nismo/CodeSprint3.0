# 📌 Zara and the Rotating Memory Chain

<p align="left">
  <img src="https://img.shields.io/badge/Difficulty-Medium-f0ad4e" />
  <img src="https://img.shields.io/badge/Topic-Doubly%20Linked%20List-blue" />
  <img src="https://img.shields.io/badge/Technique-Linked%20List%20Rotation-orange" />
</p>

---

## 📖 Problem Statement

In the futuristic kingdom of Nexora, Zara discovered a magical memory chain storing encrypted data blocks. Each block is connected to both its next and previous blocks using a doubly linked structure.

During a system reboot, the chain must be rotated to the right by **K** positions.

A right rotation means:

* The last node becomes the new head.
* Remaining nodes shift one position ahead.
* Both forward (`next`) and backward (`prev`) connections must be properly updated and maintained.

Help Zara restore the doubly linked memory chain after the rotation.

---

## 📥 Input Format

* The first line contains two integers **N** and **K**:

  * **N** → number of nodes in the doubly linked list.
  * **K** → number of rotations.
* The second line contains **N** space-separated integers representing the doubly linked list.

---

## 📤 Output Format

* Print the doubly linked list after performing the rotation from head to tail.

---

## 💡 Example

### Input

```text
N = 5, K = 2
List = 1 2 3 4 5
```

### Output

```text
4 5 1 2 3
```

---

## 📌 Constraints

| Constraint          | Value                        |
| ------------------- | ---------------------------- |
| Number of nodes     | `1 ≤ N ≤ 10^5`               |
| Number of rotations | `0 ≤ K ≤ 10^9`               |
| Node values         | `-10^9 ≤ Node values ≤ 10^9` |

---

## 💭 Hints

<details>
<summary><strong>Hint 1</strong></summary>

Avoid rotating the list one step at a time. First find the length and update: `K = K % Length`.

</details>

<details>
<summary><strong>Hint 2</strong></summary>

Make the list circular, find the new tail after rotation, and break the circle to get the new head and tail.

</details>

---

> **Tags:** `Doubly Linked List` `Linked List` `Rotation` `Simulation`

---
